
projectedSecret:
  - secret:
      name: "properties-2021-06-07"

image:
  tag: unstable-snd

env:
  APP_ENV: snd

activeProfiles: "redis,snd"

serviceAccountName: bulk-remove-users-snd

cluster: es-test

ingress:
  enabled: true
  hosts:
    - host: "bulk-remove-users-snd.apps-snd.iu.edu"
      paths: []