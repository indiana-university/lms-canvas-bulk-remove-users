
projectedSecret:
  - secret:
      name: "properties-2021-06-07"

image:
  tag: unstable-stg

env:
  APP_ENV: stg

activeProfiles: "redis,stg"

serviceAccountName: bulk-remove-users-stg

cluster: es-test

ingress:
  enabled: true
  hosts:
    - host: "bulk-remove-users-stg.apps-test.iu.edu"
      paths: []