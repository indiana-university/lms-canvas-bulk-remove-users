
projectedSecret:
  - secret:
      name: "properties-2021-06-07"

image:
  tag: unstable-reg

env:
  APP_ENV: reg

activeProfiles: "redis-session,reg,vault"

serviceAccountName: bulk-remove-users-reg

cluster: es-test

ingress:
  enabled: true
  hosts:
    - host: "bulk-remove-users-reg.apps-test.iu.edu"
      paths: []
